$C = "../../Common.ui";

Group {
  LayoutMode: Left;
  Anchor: (Bottom: 2);

  Label #PropertyKey {
    Style: (
      FontSize: 14,
      TextColor: #ffffff
    );
    Anchor: (Width: 150);
  }

  Label #PropertyValue {
    Style: (
      FontSize: 14,
      TextColor: #ffffff
    );
  }
}